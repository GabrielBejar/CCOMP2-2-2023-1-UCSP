#include <iostream>
using namespace std;

size_t getSize( ); // prototype

int main() {
    double numbers[20];
    cout << "The number of bytes in the array is ";

    cout << "\nThe number of bytes returned by getSize is " << endl;
}